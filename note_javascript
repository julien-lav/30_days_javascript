alt+7 => ` ` to use ${} 

const audio = document.querySelector(`audio[data-key="${e.keyCode}"]`);

// To stop in a function 
if(!element) return;

------
function handleValue(){...}
var inputs = document.querySelectorAll('.titles');
inputs.forEach(input => input.addEvenListener('change', handleValue));





